def twosum(arr,num):
    result= [];
    for i in range(len(arr)):
        for j in range(i+1,len(arr)):
            if(arr[i]+arr[j] == num):
                result.append([arr[i],arr[j]])
    return result
    
arr = [2,4,1,5,2,0]
num = 4
list1 = twosum(arr,num)
for i in list1:
    print(i)
